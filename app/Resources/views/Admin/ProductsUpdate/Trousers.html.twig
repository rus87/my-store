{% extends "Admin/ProductUpdate.html.twig" %}

{% block child_content %}
    <div class="container">
        <div class="col-xs-6">
            {% block common_text_fields %}
                {{ parent() }}
                <div class="form-group">
                    {{ form_label(form.waist) }}
                    {{ form_errors(form.waist) }}
                    {{ form_widget(form.waist, {'attr': {'class': 'form-control'}}) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.length) }}
                    {{ form_errors(form.length) }}
                    {{ form_widget(form.length, {'attr': {'class': 'form-control'}}) }}
                </div>

            {% endblock %}
        </div>
        <div class="col-xs-6">
            {% block photos_and_rest %}
                {{ parent() }}
            {% endblock %}
        </div>
    </div>
{% endblock %}